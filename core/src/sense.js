/**
 * Общее для всего, что процессор умеет опрашивать.
 *
 * Senseable в игре разделяет два вопроса: `senseObject` отдаёт объект, `sense` — число.
 * Отличить «свойство не объектное» от «объекта нет» нужно обоим, поэтому sentinel живёт
 * отдельно от мира: юниты опрашиваются так же, а зависеть от world.js им незачем.
 */

/** Senseable.noSensed: свойство отдаёт число, а не объект. */
export const NOT_SENSED = Symbol('notSensed')
