# Реестр сверки с игрой

Единица работы здесь — **класс игры**, а не замеченная мелочь. Для каждого класса, который мы
воспроизводим, перечислено, что из него перенесено, что расходится и что отложено. Пока строка
не закрыта, считается, что расхождение есть — даже если глазом его никто не увидел.

Обозначения: **есть** — перенесено и сверено; **расходится** — сделано иначе, чем в игре;
**нет** — не перенесено; **не нужно** — в вебе не имеет смысла.

Источник — Mindustry v159.7 и arc `208a754044`.

## logic/LCanvas.java

| Что | Состояние |
| --- | --- |
| Ширина полотна 900, узкая раскладка при 400 (`useRows`) | есть |
| Расстояние между строками 10 (`DragLayout.space`) | есть |
| Рамка строки — девятипатч `white-pane`, 5 пикселей цветом категории | есть |
| Подложка строки — чёрный на 30 процентов (`StatementElem.draw`) | есть |
| Тень под строкой: размер плюс 5, размытие 10, прозрачность 0.9 | расходится: `box-shadow` приближает `Fill.dropShadow`, точной формулы размытия у CSS нет |
| Отступ 7 **внутри** строки снизу (`marginBottom`) | есть |
| Шапка: отступ 6, имя и номер с отбивкой 8, кнопки 24 через 6 | есть |
| Имя и номер цветом категории поверх шапки того же цвета, читаемы за счёт обводки | есть |
| Тело: `pad(4).padTop(2)`, `marginLeft(4)` | есть |
| Кнопки «добавить» и «копировать» гаснут на пределе в 1000 инструкций (`LExecutor.maxInstructions`) | есть |
| Копирование правит цель перехода, если та ниже копии (`StatementElem.copy`) | не нужно: цель хранится ссылкой, вставка её не сбивает |
| Перетаскивание средней кнопкой мыши | нет: на телефоне её нет, а на сайте хватает левой |
| Перетаскивание: место вставки ищется по координате, строки расступаются | есть |
| Прокрутка полотна, пока тащишь строку у края (`LCanvas.act`) | есть: ближе 100 пикселей к краю, по 15 за кадр |
| Стрелки переходов: трапеция, вырождение в треугольник, зеркальный наконечник | есть |
| Цвет стрелки белый, при наведении `Pal.place` | есть |

## logic/LStatement.java

| Что | Состояние |
| --- | --- |
| Меню выбора: ячейки 60 на 38, по четыре в ряд (`showSelect`) | есть |
| Всплывающее меню на фоне `pane-solid`, прокрутка вместе со вкладками | есть |
| `sanitize`: правила для пустой строки, одиночной кавычки, кавычек по краям | есть |
| Карандаш выбора выравнивания (`fieldAlignSelect`): кнопка 40, ячейки 150 на 40, по три в ряд | есть |
| Фильтр выравниваний по горизонтали и вертикали (`showAlignSelect`) | не нужно: у `draw print` разрешены все девять |
| Карандаш выбора символа у `printchar`: таблица ASCII с 32 по 126, кнопки 32, по восемь в ряд | есть |
| Подсказки по наведению и по долгому нажатию (`tooltip`) | расходится: у нас `title`, долгого нажатия нет |

## logic/LogicDialog.java

| Что | Состояние |
| --- | --- |
| Меню добавления: поиск с лупой, фокус сразу, Enter добавляет первое совпадение | есть |
| Заголовки категорий: иконка, название, линия высотой 5, всё `Pal.darkishGray` | есть |
| Кнопки инструкций 130 на 50, по три в ряд, текст цветом категории | есть |
| Таблица переменных: шесть ячеек в строке, полоски по 8, высота 45 | есть |
| Обновление раз в 15 тиков, вспышка `Pal.accent` при изменении | есть |
| Окно встроенных переменных | есть |
| Строка `textBuffer` в таблице переменных | добавка: в ванили буфера не видно, идея из мода. Помечена в таблице неакцентным именем |
| Кнопка «Правка»: очистить, скопировать в буфер, загрузить из буфера, перезапустить; кнопки 280 на 60 со значком слева | есть |
| Очистка спрашивает подтверждение | есть |
| Чтение буфера обмена | расходится: браузер может не дать, тогда открывается поле для вставки руками |
| Переименование процессора мира | не нужно: процессоров мира в v1 нет |
| Ряд кнопок снизу 160 на 64 | есть |
| Разбор текста программы обратно в строки (`LCanvas.load`) | есть |

## logic/GlobalVarsDialog.java

| Что | Состояние |
| --- | --- |
| Список и порядок из `GlobalVars.init()`, разделы строками `sectionX` | есть |
| Описания из бандлов по ключам `lglobal.` | есть |
| Строка: полоска 8, имя на `Pal.gray`, описание в рамке `Tex.pane` | есть |
| Ширина описания `min(ширина экрана * 0.9 - 240, 600)` | расходится: у нас просто предел в 600 |

## ui/fragments/BlockConfigFragment.java

| Что | Состояние |
| --- | --- |
| Ряд встаёт под блоком по центру (`updateTableAlign`) | есть |
| Своего фона нет, чёрный на 60 процентов приходит от кнопок (`Styles.cleari`) | есть |
| У процессора одна кнопка — карандаш 40 на 40 | есть |
| Появление: `scaleTo(0,1)` → `scaleTo(1,1)` за 0.07 по `Interp.pow3Out` | есть |
| Исчезновение: `scaleTo(0,1)` за 0.06 | есть |
| Ряд прячется, когда блок пропал или стал недоступен | не нужно: блоки не разрушаются |

## graphics/Drawf.java

| Что | Состояние |
| --- | --- |
| `select`: квадрат, серая подложка 3 и цвет 1 | есть |
| `square`: то же четырьмя сторонами, поворот 45 | есть |
| `circles`: окружность из `11 + радиус * 0.4` отрезков | есть |
| `selected`: четыре уголка спрайтом `block-select` | нет: нужен для наведения на блок, а наведения у нас пока нет |

## world/blocks/logic/LogicDisplay.java, LogicBlock.java

| Что | Состояние |
| --- | --- |
| Все 16 команд отрисовки, упаковка в 10 бит, шаг масштаба 0.05 | есть |
| Очередь вычерпывается при отрисовке, картинка накапливается | есть |
| Картинка меньше блока: `displaySize * scaleFactor / 4` мировых единиц | есть |
| Круг дальности и рамки связей у настраиваемого процессора | есть |
| Подпись имени связи над блоком (`drawPlaceText`) | расходится: рисуем своим способом, исходник не сверялся |
| Дисплей умеет рисовать другой дисплей (`displayDrawType` 30) | нет |

## Время и анимации

| Что | Состояние |
| --- | --- |
| Тик — 1/60 реальной секунды, скачок ограничен четырьмя (`maxDeltaClient`) | есть |
| Накопитель инструкций пополняется после цикла, потолок `maxInstructionScale` | есть |
| Кривые `Interp` из arc, перевод в `linear()` для CSS | есть |
| `Actions.color`: мгновенный цвет, затем возврат по линейной кривой | есть |

## Что уже снимается генератором

Здесь расхождений не находилось ни разу: таблица приезжает из исходников, а тест следит,
чтобы код не разошёлся с таблицей.

| Данные | Генератор | Что закрывает |
| --- | --- | --- |
| Инструкции, параметры, перечисления | `gen-instructions.mjs` | состав и порядок полей |
| Логические идентификаторы контента | `gen-content.mjs` | `lookup` и счётчики |
| Переводы, описания, категории | `gen-bundles.mjs` | все тексты игры |
| Встроенные переменные и их разделы | `gen-globals.mjs` | окно встроенных переменных |
| Палитра | `gen-pal.mjs` | каждый цвет в коде |
| Иконки интерфейса | `gen-icons.mjs` | имена и коды глифов |
| Иконки контента | `gen-sprites.mjs` | меню выбора |
| Спрайты блоков | `gen-blocks.mjs` | вид мира |
| Размеры, здоровье, дальность, скорость блоков | `gen-block-specs.mjs` | модель мира и `sensor` |
| Девятипатчи: рамка, срез угла, заливка | `gen-nine.mjs` | кнопки, панели, рамка строки |
| Размеры интерфейса: 46 чисел | `gen-metrics.mjs` | раскладка полотна, строки, диалогов и меню |
| Шрифты | `gen-font.mjs` | текст в редакторе и на дисплее |

Не снимается пока: тексты интерфейса самого сайта — они наши, а не игровые, — и порядок
отрисовки, который переносится кодом, а не числом.

## Как этим пользоваться

Новая работа начинается со строки этого файла, а не с замечания. Если расхождение всё-таки
нашлось глазом — сначала сюда добавляется строка, потом чинится код: иначе тот же класс ошибок
вернётся в другом месте.

Всё перечислимое — цвета, размеры, спрайты, таблицы — должно приезжать генератором и
закрываться тестом на полноту. Там, где это сделано (инструкции, встроенные переменные, иконки,
логические идентификаторы), расхождений не находилось ни разу.
