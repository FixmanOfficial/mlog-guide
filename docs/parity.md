# Реестр сверки с игрой

Единица работы здесь — **класс игры**, а не замеченная мелочь. Для каждого класса, который мы
воспроизводим, перечислено, что из него перенесено, что расходится и что отложено. Пока строка
не закрыта, считается, что расхождение есть — даже если глазом его никто не увидел.

Обозначения: **есть** — перенесено и сверено; **расходится** — сделано иначе, чем в игре;
**нет** — не перенесено; **не нужно** — в вебе не имеет смысла.

Источник — Mindustry v160.3 и arc `68a04fab6e`.

## logic/LCanvas.java

| Что | Состояние |
| --- | --- |
| Ширина полотна: узкая 410, широкая от 400 до 1200 по экрану; порог узкой раскладки 900 на 1.2 | есть |
| Расстояние между строками 10 (`DragLayout.space`) | есть |
| Рамка строки — девятипатч `white-pane`, 5 пикселей цветом категории | есть |
| Подложка строки — чёрный на 30 процентов (`StatementElem.draw`) | есть |
| Тень под строкой: размер плюс 5, размытие 10, прозрачность 0.9 | расходится: `box-shadow` приближает `Fill.dropShadow`, точной формулы размытия у CSS нет |
| Отступ 7 **внутри** строки снизу (`marginBottom`) | есть |
| Шапка: отступ 6, имя и номер с отбивкой 8, кнопки 24 через 6 | есть |
| Имя и номер цветом категории поверх шапки того же цвета, читаемы за счёт обводки | есть |
| Тело: `pad(4).padTop(2)`, `marginLeft(4)` | есть |
| Кнопки «добавить» и «копировать» гаснут на пределе в 1000 инструкций (`LExecutor.maxInstructions`) | есть |
| Копирование правит цель перехода, если та ниже копии (`StatementElem.copy`) | не нужно: цель хранится ссылкой, вставка её не сбивает |
| Перетаскивание средней кнопкой мыши | нет: на телефоне её нет, а на сайте хватает левой |
| Перетаскивание: место вставки ищется по координате, строки расступаются | есть |
| Прокрутка полотна, пока тащишь строку у края (`LCanvas.act`) | есть: ближе 100 пикселей к краю, по 15 за кадр |
| Стрелки переходов: трапеция, вырождение в треугольник, зеркальный наконечник | есть |
| Цель перехода выбирается перетаскиванием узла на строку; нажатие сразу сбрасывает старую цель, кривая тянется за курсором, отпускание мимо оставляет её пустой | есть |
| `LCanvas.act`: пока строку тащат ближе 100 пикселей к краю, полотно едет на 15 за тик | есть, с поправкой на страницу: в окне игры прокручивается полотно, а в уроке оно не прокручивается вовсе — тогда едет страница, и строка держится под пальцем |
| На свою же строку указать нельзя (`!isDescendantOf(elem)`) | есть |
| Подсветка строки под курсором при выборе цели (`canvas.hovered`) | есть |
| Цвет стрелки белый, при наведении `Pal.place` | есть |

## logic/LStatement.java

| Что | Состояние |
| --- | --- |
| Меню выбора: ячейки 60 на 38, по четыре в ряд (`showSelect`) | есть |
| Поле 180 на 40, подписанное — тоже 180, подпись с отбивкой 10 | есть: размеры снимает `gen-metrics`, вёрстка берёт их переменными |
| В узкой раскладке подпись встаёт **после** поля (`nameAfterField`), отступы 4 и 12 | нет: у нас подпись всегда слева, переносится только строка |
| Всплывающее меню на фоне `pane-solid`, прокрутка вместе со вкладками | есть |
| `sanitize`: правила для пустой строки, одиночной кавычки, кавычек по краям | есть |
| Карандаш выбора выравнивания (`fieldAlignSelect`): кнопка 40, ячейки 150 на 40, по три в ряд | есть |
| Фильтр выравниваний по горизонтали и вертикали (`showAlignSelect`) | не нужно: у `draw print` разрешены все девять |
| Карандаш выбора символа у `printchar`: таблица ASCII с 32 по 126, кнопки 32, по восемь в ряд | есть |
| Подсказки по наведению и по долгому нажатию (`tooltip`) | есть: своя плашка, порог долгого нажатия 430 мс |
| Размеры кнопок выбора у отдельных инструкций (`radar` 110 и 180, `ucontrol` 180, мировые 100–240) | расходится: перенесены только у `op`, `draw`, `control`, `lookup` и условия; остальные идут общей шириной |
| Высота ячейки в меню выбора у `draw` и `control` — 50, а не общие 38 | нет |

## ui/dialogs/BaseDialog.java

| Что | Состояние |
| --- | --- |
| Заголовок по центру цветом `Pal.accent` и полоса под ним высотой 3 с отступом 4 | есть |
| Рамки у окна нет: фон `window-empty` прозрачный, затемнение позади `black9` | есть |
| Кнопка «Назад» 210 на 64 | есть |
| Диалог ставит игру на паузу, окно переменных её снимает | нет: мир идёт всегда |
| Звук `Sounds.uiBack` при закрытии | нет: звуков на сайте нет вовсе |

## logic/LogicDialog.java

| Что | Состояние |
| --- | --- |
| Меню добавления: поиск с лупой, фокус сразу, Enter добавляет первое совпадение | есть |
| Заголовки категорий: иконка, название, линия высотой 5, всё `Pal.darkishGray` | есть |
| Кнопки инструкций 130 на 50, по три в ряд, текст цветом категории | есть; на узком экране колонка ужимается — 3 x 130 в пиксель CSS не влезают, а в игре единица интерфейса крупнее точки экрана |
| Таблица переменных: шесть ячеек в строке, полоски по 8, высота 45 | есть |
| Обновление раз в 15 тиков, вспышка `Pal.accent` при изменении | есть на полном ходу; на замедлении каждый кадр, иначе замедление бессмысленно |
| Окно встроенных переменных | есть |
| Строка `textBuffer` в таблице переменных | добавка: в ванили буфера не видно, идея из мода. Помечена в таблице неакцентным именем |
| Длинное значение прокручивается вбок | добавка: в игре метка переносится по словам внутри своих 140 и упирается в высоту строки, целиком её не увидеть. В уроках про печать смотрят именно в эту ячейку |
| Подсказка держится за строку при прокрутке страницы и прячется, когда строка ушла с экрана | добавка: в игре сцена не прокручивается отдельно от подсказки, и такого случая не бывает |
| Подсветка строки, которая пойдёт следующей | добавка: в игре программа не подсвечивается вовсе, нужна для пошагового разбора |
| Кнопка «Правка»: очистить, скопировать в буфер, загрузить из буфера, перезапустить; кнопки 280 на 60 со значком слева | есть |
| Очистка спрашивает подтверждение | есть |
| Чтение буфера обмена | расходится: браузер может не дать, тогда открывается поле для вставки руками |
| Переименование процессора мира | не нужно: процессоров мира в v1 нет |
| Ряд кнопок снизу 160 на 64: «Назад», «Правка», «Добавить» | есть |
| Кнопка «Добавить» стоит в ряду окна, а не в полотне, и гаснет на пределе | есть |
| Четвёртая кнопка ряда, «Переменные» | не нужно: таблица переменных всё время на странице |
| Разбор текста программы обратно в строки (`LCanvas.load`) | есть |

## world/blocks/logic/MessageBlock.java

| Что | Состояние |
| --- | --- |
| Текст принимается до 400 знаков и до 24 переводов строки, по краям обрезается | есть |
| `printflush` чистит буфер даже при неподходящей цели | есть |
| Текст рисуется **под блоком**: шрифт в четверть, подложка чёрным на 20 процентов, перенос по 90 единицам, «<пусто>» серым | есть; в игре это часть выделения и видно при наведении, у нас видно всегда — в примерах курсора нет |

## logic/GlobalVarsDialog.java

| Что | Состояние |
| --- | --- |
| Список и порядок из `GlobalVars.init()`, разделы строками `sectionX` | есть |
| Описания из бандлов по ключам `lglobal.` | есть |
| Строка: полоска 8, имя на `Pal.gray`, описание в рамке `Tex.pane` | есть |
| Ширина описания `min(ширина экрана * 0.9 - 240, 600)` | расходится: у нас просто предел в 600 |

## ui/fragments/BlockConfigFragment.java

| Что | Состояние |
| --- | --- |
| Ряд встаёт под блоком по центру (`updateTableAlign`) | есть |
| Своего фона нет, чёрный на 60 процентов приходит от кнопок (`Styles.cleari`) | есть |
| У процессора одна кнопка — карандаш 40 на 40 | есть |
| Появление: `scaleTo(0,1)` → `scaleTo(1,1)` за 0.07 по `Interp.pow3Out` | есть |
| Исчезновение: `scaleTo(0,1)` за 0.06 | есть |
| Ряд прячется, когда блок пропал или стал недоступен | не нужно: блоки не разрушаются |

## world/blocks/logic: сообщение, тумблер, память

| Что | Состояние |
| --- | --- |
| Сообщение правится руками: карандаш в ряду настройки, поле 380 на 160, счётчик «сколько из 400» | есть |
| Правка строже, чем `printflush`: пробелы по краям срезаются, переносов остаётся не больше 24 | есть |
| Щелчок по тумблеру сразу его переключает (`configTapped`) | есть |
| Включённый тумблер рисуется спрайтом `switch-on` поверх основного | есть |
| Дверь открывается щелчком (`Door.tapped`), и порог у щелчка свой — 60 тиков против 80 у логики | есть |
| Открытая дверь рисуется отдельным спрайтом `door-open` | есть |
| Закрыть дверь, под которой стоят юниты, нельзя | не нужно: юнитов в v1 нет |
| Просмотр содержимого памяти | добавка: в игре посмотреть ячейку нельзя вовсе |

## world/blocks/production: Drill, GenericCrafter

| Что | Состояние |
| --- | --- |
| Бур считает руду под собой и выбирает добычу по `lowPriority`, числу клеток и номеру | есть |
| `getDrillTime`: время на предмет по твёрдости и множителю предмета | есть |
| Прогрев (`warmupSpeed`), накопление хода по числу клеток, выпуск пачкой при перепрыге | есть |
| Ускорение водой (`liquidBoostIntensity`) | нет: жидкостей в модели нет вовсе, множитель всегда единица |
| Фабрика: рецепт, `craftTime`, списание сырья, выпуск, прогрев | есть |
| `shouldConsume`: стоять, если готовому некуда деться | есть |
| Выпуск жидкостей у фабрик (`outputLiquids`) | нет: см. жидкости |
| `dump` и `offload` по соседям в порядке `Edges`, с указателем `cdump` | есть |
| Приём: склад берёт что угодно, прочие — только то, что потребляют | есть |
| Потребление энергии | нет: энергосети в модели нет, блок работает так, будто питание есть |
| Конвейеры: лента с координатами предметов, вход сзади и сбоку, пропускная способность | есть |
| Маршрутизатор: круговая раздача, возврат отправителю, задержка перед таким же маршрутизатором | есть |
| `Binding.rotate` (колесо) крутит ставящийся блок, `Binding.rotatePlaced` (R) с колесом — уже поставленный, если у него `quickRotate` | есть; когда ставить нечего и R не зажата, колесо листает страницу — на сайте иначе нельзя |
| Перекрёсток: четыре очереди по сторонам, задержка `speed` тиков, отказ при отсутствии выхода | есть, тестом |
| Сортировщик и обратный: свой предмет насквозь, чужой вбок, чередование сторон, запрет цепочки из трёх мгновенных | есть, тестом |
| Ворота переполнения и недополнения | есть, тестом |
| Настройка сортировщика предметом: сетка иконок под блоком, повторный щелчок снимает выбор | есть; поиска по названию нет — у нас двадцать два предмета, а не сотня |
| Блоки песочницы: источник предметов (сто в секунду, настраивается предметом) и предметный вакуум | есть, тестом |
| Жидкостные и энергетические источники с вакуумами, источник и вакуум грузов | нет: ни жидкостей, ни энергии, ни грузов в модели нет вовсе |
| Протоки, мосты, разгрузчики, наложенный конвейер | нет: у каждого своя механика, переносятся следующими |
| Наложенный конвейер (`StackConveyor`) и его пачки | нет |

## power/PowerGraph, PowerNode, PowerGenerator

| Что | Состояние |
| --- | --- |
| Граф сети: слияние при постройке, разрез при сносе, три роли зданий | есть, тестом |
| Баланс за такт: произведено, запрошено, покрытие всем поровну | есть, тестом |
| Батареи: заряд и разряд одинаковой долей, `@powerNetStored` и `@powerNetCapacity` | есть, тестом |
| Мачты: автосвязь при постройке в обе стороны, предел `maxNodes`, дальность до прямоугольника | есть, тестом |
| Настройка связей руками (щёлкнуть по мачте, потом по зданию) | нет: связи только автоматические |
| Сжигатель: порция сырья на `itemDuration` тиков, множитель мощности по горючести | есть, тестом |
| Солнечная панель | есть; света в модели нет, поэтому она всегда выдаёт свою мощность — по умолчанию в игре выходит то же самое |
| Тепловой генератор, РИТЭГ, ударный реактор, реакторы | нет: им нужны тепло карты, жидкости и охлаждение |
| Диоды, лучевые узлы Эрекира, перегрузчик (`overdrive`) | нет |
| Луч связи: цвет от белого к `Pal.powerLight` по нехватке, прозрачность 0.5 | есть; рисуется линией, а не спрайтами `laser` и `laser-end` |

## Перевод интерфейса логики (v160)

| Что | Состояние |
| --- | --- |
| Названия инструкций, подписи параметров и значения перечислений переводятся в редакторе (`instruction.*`, `name.token.*`, `<enum>.label.*`) | есть; у операций переводятся только те знаки, что написаны словом — `and`, `or`, `not`, `xor`, `flip`, `always`, как в `selectTranslate` |
| Настройка `logiclocalization`, по умолчанию включена | есть: кнопка с книгой у примера и тумблер «перевод» в песочнице, выбор запоминается на весь сайт |
| Сам язык, схемы и значения полей остаются английскими | есть: у нас и не было иначе |
| Камера (`@cameraX` и соседние) у обычного процессора | есть: отвечает `null`. Самой камеры в модели нет, поэтому у мирового процессора ответ тот же |

## logic/LExecutor: UnitBindI, UnitControlI

| Что | Состояние |
| --- | --- |
| `ubind` ходит по юнитам типа по кругу, счётчик у процессора свой на каждый тип | есть |
| Привязка к юниту-объекту: только своей команды и только `logicControllable` | есть |
| Привязка к `null` и к неуправляемому типу обнуляет `@unit` | есть |
| `checkLogicAI`: контроллер создаётся при первой команде и чистит добычу и стройку | есть |
| Каждый `ucontrol` продлевает контроль на 600 тиков | есть |
| Координаты приходят в тайлах и переводятся `World.unconv` | есть |
| `move`, `stop`, `approach`, `pathfind`, `idle`, `autoPathfind`, `boost`, `flag`, `within`, `unbind` | есть |
| `target` и `targetp` пишут цель и флаг стрельбы | есть: поля пишутся, стрельбы в песочнице нет |
| `getBlock` | расходится: блок и здание отдаются, третий результат всегда пуст — пола и руды у нас нет |
| `mine`, `itemDrop`, `itemTake` | есть |
| `build`, `deconstruct`, `payDrop`, `payTake`, `payEnter` | нет: нужны планы постройки и грузы. Сборщик ставит диагностику, а не пропускает молча |
| Конфигурация в `build`: пятый параметр кладётся в план и доезжает до здания | нет, вместе с самой `build`. Это единственный способ поставить логикой мост или узел со связями: `setblock` ставит здание, но настроить его не может |
| `radar` и `uradar` целиком, с кешем на 30 тиков | есть |
| `ulocate ore` | есть |
| `ulocate building`, `spawn`, `damaged` | нет: нужны флаги зданий, точки появления волн и учёт повреждений |
| Правила `logicUnitControl`, `logicUnitBuild`, `logicUnitDeconstruct` | не нужно: правил мира в песочнице нет |

## ai/types/LogicAI.java

| Что | Состояние |
| --- | --- |
| Таймаут 600 тиков, задержка передачи 90 | есть |
| `move` → `moveTo(цель, 1, 30)` | есть |
| `approach` → `moveTo(цель, moveRad - 7, 7, keepDistance)` | есть |
| `pathfind` для летающих — прямо к цели | есть |
| `pathfind` для наземных через `ControlPathfinder` | расходится: идут напрямую, поиска пути нет |
| `autoPathfind`: ближайшее вражеское ядро, спавны, поля потоков | нет: ядер и волн в песочнице нет |
| Подъём и снижение по `riseSpeed` и `descentSpeed` для умеющих зависать | есть |
| Поворот в сторону движения, когда не во что целиться | есть |
| `radars`, `checkTargetTimer` — сброс кеша целей раз в 40 тиков | есть: таймер идёт, целей пока нет |
| Выбор цели и стрельба | нет: оружия и пуль в v1 нет |

## entities/units/AIController.java, entities/comp: UnitComp, VelComp

| Что | Состояние |
| --- | --- |
| `moveTo` целиком: гашение у цели, разворот при `length < -0.5`, отсечка негодных значений | есть |
| `arrive` и `offset` в `moveTo` | не нужно: `LogicAI` их не использует |
| `movePref` → `moveAt` для всенаправленных, `rotateMove` для прочих | есть |
| `moveAt`: шаг скорости ограничен `accel * длина вектора * delta` | есть |
| `VelComp.update`: перенос до трения, приоритет -1 | есть |
| Обнуление скорости при столкновении (`Mathf.equal(px, x)`) | нет: столкновений с блоками у нас нет |
| Трение от пола и правил мира | не нужно: полов нет, множители единичны |
| `speed()`: штраф за движение боком и надбавка за подъём | расходится: штраф получает только игрок, а игрока в песочнице нет |
| `MechComp`: свой угол ног, фаза шага от пройденного пути, замена `rotateMove` | есть |
| Границы карты, утопление, урон от пола | нет |
| Физика столкновений юнитов между собой (`updatePhysics`) | нет |

## type/UnitType.java

| Что | Состояние |
| --- | --- |
| Скорость, ускорение, трение, поворот, размер, здоровье, броня, полёт | есть, генератором |
| Вместимость, выведенная из размера в `init()` | есть, генератором |
| `range` и `maxRange`, выведенные из дальности пуль | есть: сняты дампом из запущенной игры |
| `sense` у самого типа (`sensor result @dagger @health`) | нет |
| Оружие, способности, слои отрисовки, эффекты | не нужно: логике не видно |

## Отрисовка юнита: UnitType.draw

| Что | Состояние |
| --- | --- |
| Корпус под углом `rotation - 90` | есть |
| Обводка корпуса (`Pixmaps.outline`, радиус 3, `Pal.darkerMetal`) | есть: снимается готовой из атласа игры |
| Накладка `-cell` цветом команды (`drawCell`) | есть |
| Пульсация цвета ячейки от здоровья (`cellColor`) | нет: у целого юнита он и так равен цвету команды |
| Юниты рисуются поверх зданий | есть: слой 60 против 30 |
| Ноги меха и основание (`drawMech`): вынос по фазе шага, наклон при подъёме, подкраска передней ноги | есть |
| Качание корпуса при ходьбе, вбок и вперёд (`legOffset`) | есть |
| Огонь двигателей (`drawEngines`) | есть |
| След за летящим (`trail`) | нет |
| Гусеницы танков (`drawTank`): кадры и полосы, нарезанные упаковщиком | нет |
| Ноги пауков (`drawLegs`): обратная кинематика в `LegsComp` | нет: отдельная подсистема с шаганием по опорам |
| Тень под юнитом (`drawShadow`) | нет |
| Оружие поверх корпуса (`drawWeapons`) | нет: оружия в v1 нет |
| Полоса здоровья и значок команды над юнитом | нет |

## world/blocks/environment: Floor, OreBlock, StaticWall

| Что | Состояние |
| --- | --- |
| Пол, наложение и статичная стена как три слоя тайла | есть |
| Вариант плитки по `Mathf.randomSeed` от координат | есть |
| Мягкие края переходов: лист 3 на 3 из стенсиля, выбор по направлению соседа | есть |
| Порядок краёв по идентификатору блока (`doEdge`) | есть |
| Руда поверх пола, без своих краёв | есть |
| Множители скорости и трения от пола | есть |
| Плиточные полы (`tilingVariants`: `metal-tiles`) | нет: своя раскладка большим полотном |
| Автотайловые полы и вода (`autotile`, `ShallowLiquid`) | нет: 47 состояний по битовой маске соседей |
| Обрывы (`Cliff`), деревья и валуны (`Prop`, `TallBlock`) | нет: украшение, в атлас не берём |
| Большие спрайты стен `-large` для скоплений 2 на 2 | нет |
| Анимация жидкостей и эффекты пола (`updateRender`) | нет |
| Тень под статичной стеной | нет |

## logic: sensor у контента и цвета

| Что | Состояние |
| --- | --- |
| `sensor` на самом типе: здоровье, размер, дальность, вместимость, скорость | есть |
| `@color` у здания и юнита — цвет команды, упакованный `Color.toDoubleBits` | есть |
| `@color` у предмета и жидкости — их собственный цвет | есть |
| `@color` у типа блока — цвет на карте из `block_colors.png` | есть |
| `@id` — логический идентификатор из `logicids.dat` | есть |

## logic: инструкции процессора мира

Двадцать шесть привилегированных инструкций. Перенесено восемнадцать, осталось восемь.

| Что | Состояние |
| --- | --- |
| Привилегия по `Block.privileged`, проверка в каждой инструкции | есть |
| Меню добавления: у процессора мира к обычным инструкциям добавляются мировые | есть, вместе со скрытыми и правилом `logicUnitControl` |
| Круг дальности у привилегированного процессора не рисуется | есть: у него дальность `Float.MAX_VALUE` |
| `getflag`, `setflag` — флаги целей в правилах мира | есть |
| `setrule` со всеми мерами, включая `ban`/`unban` и `mapArea` | есть: значения хранятся, но волны, освещение и множители мы не считаем |
| `getblock`, `setblock` по всем четырём слоям | есть |
| `spawn` | есть, кроме случайного сдвига на ±0.01 |
| `setrate`, `sync` | есть; `sync` в одиночной игре не делает ничего и в игре тоже |
| `fetch` — перебор юнитов, зданий и ядер команды | есть; игроков нет, поэтому `player` отвечает пусто |
| `setprop` у юнита и здания, включая количество предмета | есть |
| `localeprint` из словаря карты | есть: словарь пустой, пока карту не загрузили |
| `clientdata` | есть: как и `sync`, в одиночной игре не делает ничего |
| `explosion` лучами и сплошным кругом, с бронёй и спадом | есть |
| `status`: наложение и снятие эффектов, множители скорости, трения и получаемого урона, урон и лечение по времени | есть |
| `bullet` | нет: пуль в модели нет вовсе |
| `spawnwave`, `weathersense`, `weatherset` | нет: волн и погоды в песочнице нет |
| `message` со всеми четырьмя видами, включая отказ при занятом экране | есть: сообщение становится состоянием мира, страница его показывает |
| `query` кругом и прямоугольником, по юнитам и зданиям | есть; пули не ищет и сама игра |
| `cutscene`, `playsound`, `playmusic` | нет: это интерфейс игры, не мира |
| `makemarker` семи видов, с `replace` и потолком в 20 000 меток | есть |
| `setmarker` — двадцать шесть управлений, у каждого вида свой набор | есть; `remove`, `flushText` и `texture` тоже |
| `effect` | нет: эффектов в модели нет вовсе |

## game/MapObjectives.java: метки

Метки — общая часть с целями карты: цель несёт те же классы, и рисуются они тем же кодом.
Перенос в `core/src/markers.js`, отрисовка в `render/src/world.js`.

| Что | Состояние |
| --- | --- |
| Общее у всех: `world`, `minimap`, `autoscale`, `drawLayer` | есть; миникарты у нас нет, и `minimap` ни на что не влияет |
| NaN в первом параметре — «ничего не делать», в остальных — «не трогать это свойство» | есть |
| Чужое свойство не делает ничего: у подписи нет радиуса, у линии поворота | есть, тестом |
| `shapetext`: многоугольник, подпись над ним, флаги подложки и обводки | есть |
| `point`: круг, разбегающийся от центра за сто тиков | есть; время берётся из тика мира, а не из часов браузера |
| `shape`: полый и залитый, дугой и целиком; третий параметр `shape` гасит обводку | есть |
| `text`: подпись с выравниванием по обеим осям | есть; строка одна, многострочную раскладку в игре считает `GlyphLayout` |
| `line`: два цвета концов, `colori` по номеру, `posi` по номеру точки | есть |
| `texture`: картинка по имени | модель есть, отрисовки нет: нужен произвольный регион атласа по имени |
| `quad`: четыре вершины со своими координатами, `uv` и цветом | модель есть, отрисовки нет: та же причина |
| `pos` у `quad` берёт **обе** координаты из первого параметра | перенесено как есть: это ошибка игры, но карты написаны под неё |
| `WorldLabel.drawAt`: масштаб 0.25 от размера, подложка 0.3 чёрного, обводка каймой в две единицы | есть |
| Потолок в 20 000 меток и `replace` у `makemarker` | есть |

## game/MapObjectives.java: цели

Тринадцать условий и то, что их проверяет. Перенос в `core/src/objectives.js`, счётчики
партии — в `core/src/stats.js`.

| Что | Состояние |
| --- | --- |
| Порядок: цели проверяются **до** юнитов, зданий и процессоров | есть: `Logic.update` зовёт их перед `updateEntities` |
| `done` один раз: снимает и ставит флаги, запускает `completionLogicCode` | есть |
| Цепочка родителей: потомок не проверяется, пока родитель не выполнен | есть, вместе с разворачиванием потомков в общий список |
| `flag` — ждёт флага из `setflag` | есть |
| `timer` — копит время, слушается `objectiveTimerMultiplier` | есть |
| `destroyBlock`, `destroyBlocks` — проверяют несовпадение, а не разрушение | есть |
| `destroyCore` — у команды волн не осталось ядер | есть |
| `destroyUnits` — счётчик сбитых чужих юнитов | есть |
| `unitCount` — юниты своей команды по типу | есть |
| `item` — предметы на складе первого ядра команды | есть |
| `commandMode` — без интерфейса игры засчитывается сразу | есть: у нас интерфейса игры нет вовсе |
| `coreItem` — предметы, доехавшие до ядра **транспортом** | условие есть, счётчик пуст: транспортных блоков в модели нет, а `handleStack` его не трогает и в игре |
| `buildCount` — построенные блоки | условие есть, счётчик пуст: постройки в модели нет |
| `research`, `produce` — открытый контент | условие есть, читает набор `world.unlocked`: дерева технологий у нас нет |
| Текст условия | ядро отдаёт ключ строки и числа, строку собирает страница |
| Счётчики партии: сбитые юниты, разрушенные блоки, созданные юниты | есть, по тем же событиям, что в `Logic` |


## ui: HUD игры

Пока перенесены три угла из четырёх. Панель строительства и панель волн — следующие.

| Что | Состояние |
| --- | --- |
| Полоса состояния: миссия перекрывает цели, цели идут списком, каждая строка с `[white]` | есть |
| Цветная разметка текста (`[accent]`, `[]`, `[#rrggbb]`, `[[`) со стеком цветов | есть, тестом |
| Таблица имён цветов: `arc.graphics.Colors` плюс пять из `UI.init` | есть: снимает `gen-markup.mjs`, 81 имя |
| `message`: объявление по центру, подсказка сверху, уведомление панелью кнопки | есть; звуков нет |
| Миникарта: цвет тайла по `MapIO.colorFor`, юниты точками команды | есть; приближения и перетаскивания нет |
| Координаты клетки под курсором | есть; координат игрока нет — игрока в песочнице нет вовсе |
| Запасы ядра: сетка по четыре, предмет остаётся показанным навсегда | есть |
| `UI.formatAmount`: «39к», пороги 1000 и 10000 | есть, тестом |
| Панель строительства целиком: заголовок в `Tex.buttonEdge2`, сетка в `Tex.pane2`, серая линия высотой 4, ряд кнопок по 48, категории по две в ряд | есть |
| Рамка выбранного блока — `button-select` (`Styles.selecti`), выбранной категории — `flat-down-base` | есть: девятипатчи сняты генератором |
| Неполный ряд сетки добивается пустыми клетками, чтобы панель не меняла размер | есть |
| Подсказка клавиш выбора под названием блока | есть; клавиши берутся из снятых привязок |
| Меню выбора не выше и не шире экрана, `keepInStage` двигает его внутрь | есть; сверх игры меню поднято над шапкой сайта и считает экран по `visualViewport` — на телефоне панель браузера занимает часть окна, а игре такого знать не нужно |
| Подсказка к параметру: сразу по наведению, по долгому нажатию на телефоне, фон `black8`, текст `[lightgray]` с обводкой, смещение 15 и 19 от курсора | есть; появляется только там, где ключ есть в бандле игры — у `<` и `>` подсказки нет и в игре |
| `Styles.logicTogglet`: выбранный и нажатый пункт списка акцентные, обычный чёрный | есть; наведение подсвечивает невыбранное — в игре у кнопки состояния `over` нет вовсе, но на странице без него не видно, куда наводишь |
| Ряд под сеткой: у настольного ввода схемы и база данных, у мобильного снос, диагональ, поворот, подтверждение | частично: снос и поворот. Схем и базы данных у нас нет, диагональ и подтверждение — про планы постройки, а постройка мгновенная |
| Выбор блока с клавиатуры: цифра категории, цифра блока, третья цифра как десятки, стрелки по сетке | есть, тестом |
| Поворот `R` (`Binding.rotatePlaced`), правая кнопка снимает выбор, потом сносит | есть; правая кнопка сносит протяжкой, областью |
| `Placement.normalizeLine`: линия идёт по той оси, вдоль которой протянули дальше | есть, тестом |
| `InputHandler.iterateLine`: поворот каждого блока — направление на следующий; у последнего берётся от предыдущего (`conveyorPlacement`), у турелей поворот линии не трогается (`ignoreLineRotation`), крупные блоки идут через свой размер | есть, тестом |
| Замена блока блоком (`Block.canReplace`, `Build.validPlace`): конвейер поверх конвейера ради поворота, крупная стена поверх мелких | есть, тестом; наложенный конвейер из правила выпал — его в модели нет |
| Постановка зажатой кнопкой: пока тянут, под курсором призраки будущей линии со своим поворотом у каждого; блоки появляются по отпусканию | есть; планов постройки нет — постройка мгновенная, поэтому призрак и есть весь план |
| Снос протяжкой выделяет прямоугольник (`InputHandler.drawBreakSelection`) | есть; правой кнопкой и в режиме сноса |
| Постановка пальцем: в игре у мобильного ввода свой режим с подтверждением | нет: палец тянет ту же линию, что и мышь, пока выбран блок |
| Поворот хранится у здания, виден `sensor @rotation` и рисуется на карте | есть |
| `Autotiler`: конвейер выбирает кусок ленты по соседям — прямая, угол, тройник, крест — и отражает его при надобности | есть, тестом |
| Кадр хода ленты: `(Time.time * speed * 8 * timeScale * efficiency) % 4`, а при заторе кадр нулевой | есть; затора (`clogHeat`) в модели нет, лента всегда идёт |
| Кусок ленты, дорисованный поверх соседа с неквадратным спрайтом (маска `blendresult[4]`) | нет: неквадратные спрайты только у протоков, а их в модели нет |
| Спрайты всех строимых блоков на карте | есть: 261 спрайт своим размером, как рисует игра |
| `drawTeamTop`: накладка команды поверх блока | есть: у команды с палитрой берётся её собственный спрайт `<имя>-team-<команда>` и рисуется как есть, у остальных общий `<имя>-team` красится цветом команды |
| Поставленный процессор становится настоящим: пустая программа, скорость из спеки, место в списке | есть |
| Постановка блока щелчком и снос правой кнопкой | есть; щелчок — частный случай протяжки, линия в одну клетку. Проверки места — часть `Build.validPlace`, см. `world.canPlace` |
| Верхняя таблица панели: выбранный блок с требованиями, иначе здание под курсором с полоской здоровья и содержимым | есть; полосок потребления и потока нет — этих систем в модели нет |
| Стоимость и время постройки | не считаются намеренно: сцена включает `infiniteResources` и `instantBuild`, то есть режим `Gamemode.sandbox` |
| Скрытие интерфейса: `HudFragment.shown`, клавиша C (`Binding.toggleMenus`) | есть, вместе с подсказкой `showui` |
| Панель волн, кнопка меню, звуки | нет: волн в модели нет вовсе |


## graphics/Drawf.java

| Что | Состояние |
| --- | --- |
| Линейная фильтрация атласа (настройка `linear`, включена по умолчанию) | есть у карты; дисплей рисуется по пикселям — у него своё разрешение |
| Кнопка выбора у `sensor` — всегда карандаш, значок не зависит от выбранного | есть |
| Escape закрывает верхнее окно (`BaseDialog.addCloseListener`) | есть: окна лежат стопкой, закрывается последнее открытое |
| Меню добавления: кнопки 130 на 50 по три в ряд, окно по содержимому, высота до 0.8 экрана | есть |
| Имена инструкций в меню и в шапке строки — от имени класса игры | есть: снимает `gen-instructions.mjs` |
| `select`: квадрат, серая подложка 3 и цвет 1 | есть |
| `square`: то же четырьмя сторонами, поворот 45 | есть |
| `selected`: уголки спрайтом `block-select`, по одному на угол с поворотом на 90 | есть; рисуются там же, где в игре — на том, что сейчас снесут |
| Курсор с выбранным блоком: призрак блока, подмешанный к белому на 0.24 с пульсацией, а на негодном месте — к `Pal.breakInvalid` на 0.4 | есть |
| Наведение на здание само по себе ничего на карте не рисует | есть: уголки при наведении были нашей выдумкой |
| `circles`: окружность из `11 + радиус * 0.4` отрезков | есть |
| `selected`: четыре уголка спрайтом `block-select` | нет: нужен для наведения на блок, а наведения у нас пока нет |

## world/blocks/logic/LogicDisplay.java, LogicBlock.java

| Что | Состояние |
| --- | --- |
| Все 16 команд отрисовки, упаковка в 10 бит, шаг масштаба 0.05 | есть |
| Очередь вычерпывается при отрисовке, картинка накапливается | есть |
| Картинка меньше блока: `displaySize * scaleFactor / 4` мировых единиц | есть |
| Круг дальности и рамки связей у настраиваемого процессора | есть |
| Подпись имени связи над блоком (`drawPlaceText`) | расходится: рисуем своим способом, исходник не сверялся |
| Дисплей умеет рисовать другой дисплей (`displayDrawType` 30) | нет |

## Время и анимации

| Что | Состояние |
| --- | --- |
| Тик — 1/60 реальной секунды, скачок ограничен четырьмя (`maxDeltaClient`) | есть |
| Накопитель инструкций пополняется после цикла, потолок `maxInstructionScale` | есть |
| Кривые `Interp` из arc, перевод в `linear()` для CSS | есть |
| `Actions.color`: мгновенный цвет, затем возврат по линейной кривой | есть |

## Что уже снимается генератором

Здесь расхождений не находилось ни разу: таблица приезжает из исходников, а тест следит,
чтобы код не разошёлся с таблицей.

| Данные | Генератор | Что закрывает |
| --- | --- | --- |
| Инструкции, параметры, перечисления | `gen-instructions.mjs` | состав и порядок полей |
| Логические идентификаторы контента | `gen-content.mjs` | `lookup` и счётчики |
| Переводы, описания, категории | `gen-bundles.mjs` | все тексты игры |
| Встроенные переменные и их разделы | `gen-globals.mjs` | окно встроенных переменных |
| Палитра | `gen-pal.mjs` | каждый цвет в коде |
| Иконки интерфейса | `gen-icons.mjs` | имена и коды глифов |
| Иконки контента по цепочке `loadIcon` | `gen-sprites.mjs` — **из атласа игры** | меню выбора и `draw image` |
| Спрайты блоков и накладки команд | `gen-blocks.mjs` — **из атласа игры** | вид мира |
| Спрайты юнитов с обводкой и накладки команд | `gen-units.mjs` — **из атласа игры** | вид мира |
| Плитки местности, руды, стены и листы краёв | `gen-terrain.mjs` — **из атласа игры** | вид мира |
| Цвета шести базовых команд | `gen-dump.mjs` | накладка юнита, `sensor @color` |
| Спеки всех 446 блоков и 69 юнитов | `gen-dump.mjs` — **запуском игры** | модель мира, `sensor`, каталог для постройки |
| Полная опись характеристик всего контента | `gen-dump.mjs`, отражением | справочник: числа, которые мы не моделируем |
| Девятипатчи: рамка, срез угла, заливка | `gen-nine.mjs` | кнопки, панели, рамка строки |
| Размеры интерфейса: 46 чисел | `gen-metrics.mjs` | раскладка полотна, строки, диалогов и меню |
| Шрифты | `gen-font.mjs` | текст в редакторе и на дисплее |

Не снимается пока: тексты интерфейса самого сайта — они наши, а не игровые, — и порядок
отрисовки, который переносится кодом, а не числом.

## Как этим пользоваться

Новая работа начинается со строки этого файла, а не с замечания. Если расхождение всё-таки
нашлось глазом — сначала сюда добавляется строка, потом чинится код: иначе тот же класс ошибок
вернётся в другом месте.

Всё перечислимое — цвета, размеры, спрайты, таблицы — должно приезжать генератором и
закрываться тестом на полноту. Там, где число не записано в исходнике, а выводится в `init()`,
генератор читает не текст, а запущенную игру: `gen-dump.mjs`. Там, где это сделано (инструкции, встроенные переменные, иконки,
логические идентификаторы), расхождений не находилось ни разу.
