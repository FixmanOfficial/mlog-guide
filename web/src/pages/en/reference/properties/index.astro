---
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro'
import PropertiesPage from '../../../../reference/PropertiesPage.astro'
import {strings} from '../../../../reference/strings.js'

const headings = [{depth: 2, slug: 'controlled', text: strings('en').controlled}]
---
<StarlightPage frontmatter={{
    title: 'Sensor properties',
    description: 'Which property is readable on what, and what it returns'
}} headings={headings}>
    <PropertiesPage locale="en" />
</StarlightPage>
