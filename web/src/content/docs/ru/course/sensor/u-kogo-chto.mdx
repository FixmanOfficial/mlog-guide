---
title: Свойство есть не у каждого
description: Почему @ammo работает только у турели, чем тип блока отличается от здания и что делать с пустыми ответами
lead: Почему @ammo есть только у турели, а @progress — у бура, и чем тип блока отличается от здания
difficulty: medium
sidebar:
    order: 4
---

import {Example} from '../../../../../example/Example.jsx'
import LessonHead from '../../../../../course/LessonHead.astro'
import '../../../../../example/example.css'
import {HOLDERS} from '../../../../../course/scenes/sensor.js'

<LessonHead difficulty="medium" needs={[
    {lesson: 'sensor/zdaniya', title: 'sensor: свойства зданий'},
    {lesson: 'sensor/predmety', title: 'sensor: предметы и пустота'}
]} />


Свойств 77, но ни у кого нет всех сразу. Это не ограничение логики, а устройство игры:
свойство читает код конкретного класса, и `@ammo` написан в турели, `@progress` — в буре
и фабрике, `@memoryCapacity` — в ячейке памяти. У остальных этого кода просто нет.

<Example client:only="preact" scene={HOLDERS} />

Здесь дуо подбито до 120 из 250, а память есть у ячейки и нет у маршрутизатора.

## Тип и здание — разные собеседники

Первые две строки спрашивают одно и то же свойство, но у разных вещей:

- `@duo` — **тип блока**, чертёж. Он отвечает, каким дуо бывает: 250 здоровья, размер 1.
- `duo1` — **здание**, конкретная турель на карте. Она отвечает, какая она сейчас: 120.

У типа поэтому не бывает ни `@totalItems`, ни `@enabled`: чертёж ничего не содержит
и не работает. Зато у него есть `@id` и `@name` — у здания их нет, — и ещё цена:

```
sensor медь @duo @copper       # 35: столько меди стоит дуо
sensor титан @vault @titanium  # 250
```

Предмет третьим аргументом у здания означает «сколько его внутри», а у типа — «сколько его
стоит». Из логики цену больше узнать нечем; в режиме бесконечных ресурсов она обнуляется.

## Кто на что отвечает

Общее для всех зданий — здоровье, координаты, команда, размер, содержимое. Дальше начинается
частное:

| Свойство | У кого |
| --- | --- |
| `@ammo`, `@ammoCapacity`, `@currentAmmoType` | турели, 26 блоков |
| `@progress` | всё, что делает работу за время: буры, фабрики, стройка |
| `@heat` | реакторы, нагреватели, проектор щита |
| `@memoryCapacity` | ячейка и банк памяти |
| `@displayWidth`, `@displayHeight` | дисплеи |
| `@totalLiquids` | у фабрики — только её выходная жидкость, у насоса — только та, что он качает |

Полный список, свойство за свойством, — в [таблице свойств](/ru/reference/properties/):
у каждого написано, у кого оно читается и что отдаёт. Эта таблица снята из кода игры,
а не составлена вручную, поэтому ей можно верить.

## Что делать с пустым ответом

Никакой ошибки не будет: `sensor` не жалуется, а кладёт `null`. Программа продолжит работать,
и на обычном сравнении пустота притворится нулём — те же грабли, что
[в уроке про значения](/ru/course/basics/znacheniya/), только теперь их ставит опечатка
в имени свойства или блок не того вида.

Отсюда правило: если проверка «патронов ноль» вдруг срабатывает всегда — сначала посмотрите,
у того ли блока вы их спрашиваете.

## Коды `@controlled`

Единственное свойство группы, которое отвечает не числом-величиной, а кодом:

| Значение | Что означает |
| --- | --- |
| 0 | никто не управляет |
| 1 | процессор (`@ctrlProcessor`) |
| 2 | игрок (`@ctrlPlayer`) |
| 3 | команда игрока через режим командования (`@ctrlCommand`) |

Сравнивать лучше с константами, а не с числами: `jump 10 equal кем @ctrlProcessor` читается,
а `equal кем 1` — нет.

:::caution
В песочнице турели не стреляют и буры не копают, поэтому `@ammo` и `@progress` здесь всегда
пустые, а в игре это числа. Всё остальное в этом уроке — настоящее.
:::

## Задание

Нажмите «Править» и допишите `sensor патроны duo1 @ammo`. В песочнице получится `null`
по причине выше, а в игре — число патронов. Потом спросите `@ammo` у маршрутизатора: там
пусто в обоих случаях, и это тот самый случай, когда пустота означает «свойство не про меня».
