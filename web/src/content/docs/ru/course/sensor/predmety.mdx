---
title: Предметы и пустота
description: Как узнать, сколько в блоке конкретного предмета, и чем ноль отличается от пустого значения
lead: Сколько в блоке меди — и чем «предмета нет» отличается от «свойства нет»
difficulty: medium
sidebar:
    order: 2
---

import {Example} from '../../../../../example/Example.jsx'
import LessonHead from '../../../../../course/LessonHead.astro'
import '../../../../../example/example.css'
import {ITEMS} from '../../../../../course/scenes/sensor.js'

<LessonHead difficulty="medium" needs={[
    {lesson: 'sensor/zdaniya', title: 'sensor: свойства зданий'},
    {lesson: 'basics/znacheniya', title: 'Число, объект и пустота'}
]} />


Третьим аргументом `sensor` берёт не только свойство, но и предмет. В хранилище 220 меди
и 15 графита; кремния нет вовсе, воды хранилище не держит в принципе.

<Example client:only="preact" scene={ITEMS} />

Три разных ответа на три похожих вопроса:

- **медь = 220** — предмет есть, вот сколько его;
- **кремний = 0** — предмет бывает, но здесь его нет;
- **вода = null** — хранилище не держит жидкости вовсе, спрашивать не о чем.

Ноль и `null` — не одно и то же. Ноль это ответ «нисколько». `null` — это «вопрос не про
меня».

Разница не косметическая: в обычном сравнении пустота притворяется нулём, и проверка
«меди не осталось» ловит заодно «блока нет» и «свойство не то». Почему так и что с этим
делать — в уроке [«Число, объект и пустота»](/ru/course/basics/znacheniya/); здесь достаточно
помнить, что `sensor` отвечает пустотой чаще, чем кажется.

## Первый предмет — объект

`@firstItem` отдаёт не количество, а сам предмет: тот же объект, что константа `@copper`.
Его удобно передавать дальше — в `ucontrol itemTake`, в `sensor` следующим аргументом,
в `print`.

«Первый» — это тот, что лежит в блоке раньше других и ещё не кончился, а не самый крупный
и не первый по алфавиту. У бура это то, что он копает.

## Что можно спросить предметом

Любой предмет и любую жидкость из игры: `@copper`, `@graphite`, `@water`, `@cryofluid`.
Полный список — в [справочнике](/ru/reference/instructions/lookup/), там же способ перебрать
их все по номеру.

:::note
Жидкости в песочнице пока не текут: блоки, которые их хранят, всегда пустые. Числа
из примеров про предметы — настоящие, про жидкости — нет.
:::

## Задание

Нажмите «Править» и спросите у хранилища ещё три вещи: `@graphite` (он там есть),
`@titanium` (такого предмета нет в хранилище, но он бывает) и `@sand`. Первый ответит числом,
остальные — нулём.

Потом замените `vault1` на `@vault` — спросите то же самое не у хранилища, а у его типа.
Ответ будет не про содержимое, а про **цену**: `sensor @vault @titanium` отдаёт 250, столько
титана стоит хранилище. У чертежа нет содержимого, зато есть стоимость, и это единственный
способ узнать её из логики.

В режиме бесконечных ресурсов цена честно отвечает нулём — как и постройка, которая ничего
не списывает.
