---
title: Set Rate
description: "Как setrate меняет скорость процессора, почему её обычно снижают и где предел @ipt"
lead: "Процессор работает на своей скорости всегда. setrate — единственный способ её изменить, и меняют её почти всегда вниз"
difficulty: medium
sidebar:
    order: 0
    label: Скорость процессора
---

import Example from '../../../../../example/Example.astro'
import LessonHead from '../../../../../course/LessonHead.astro'
import '../../../../../example/example.css'
import Card from '../../../../../course/Card.astro'
import Lesson from '../../../../../course/Lesson.astro'
import {FULL, SLOW, CEILING} from '../../../../../course/scenes/rate.js'

<LessonHead
    difficulty="medium"
    lead={frontmatter.lead}
    needs={[
        {lesson: 'basics/processor', title: 'Как работает процессор'},
        {lesson: 'wait/rhythm', title: 'Ритм программы'}
    ]}
/>

У каждого процессора своя скорость — сколько инструкций он успевает за тик. Она лежит
в `@ipt` и написана в характеристиках блока:

| Блок | Инструкций за тик |
| --- | --- |
| Микропроцессор | 2 |
| Логический процессор | 8 |
| Гиперпроцессор | 25 |
| Мировой процессор | 8, до 1000 |

<Example scene={FULL} world={true} />

Гиперпроцессор на полном ходу: за секунду программа из пяти строк проходит её около
**295 раз**. Столько же раз за секунду читается `sensor`, перерисовывается дисплей
и пересчитывается всё, что в ней написано.

## `setrate` меняет скорость на ходу

<Example scene={SLOW} world={true} />

Та же программа с одной новой строкой в начале:

```
setrate 1
```

Теперь `@ipt` равен единице, и за ту же секунду программа проходит **14 итераций** вместо
295. Она делает ровно то же самое, только в двадцать раз реже.

<Card kind="note">
`@ipt` меняется сразу же, в той же строке: инструкция правит скорость самого процессора,
а не готовит её к следующему запуску.
</Card>

## Выше своего потолка не поднять

<Example scene={CEILING} world={true} />

Здесь микропроцессору велено работать на тысяче инструкций за тик, а `скорость` всё равно
остаётся **двойкой**.

Число в `setrate` прижимается к отрезку от единицы до предела **этого блока**, и предел
у обычного процессора — его собственная скорость. Разогнать микропроцессор до гиперпроцессора
нельзя; можно только замедлить.

<Card kind="error">
**`setrate` — это тормоз, а не ускоритель.** Единственный, кому он поднимает скорость, —
<Lesson lesson="world-intro">мировой процессор</Lesson>: у него база 8, а потолок 1000.
Обычные три блока живут на своей скорости, и `setrate` умеет только сделать её меньше.
</Card>

## Зачем это вообще нужно

Честный ответ: **ради мирового процессора**. Восьми инструкций за тик мало на сценарий,
который перебирает юнитов, считает волны и разговаривает с игроком, — и `setrate 1000`
даёт ему в сто с лишним раз больше.

Обычным процессорам инструкция досталась заодно: до версии 160 она была привилегированной
и им не давалась вовсе. Применения у замедления при этом почти нет — игра спокойно тянет
сотни процессоров, и ускорять ею карту не выйдет: выше своей скорости обычный процессор
не поднимется, а ниже опускать его незачем.

Единственное, где это иногда полезно, — **разглядеть работу программы**: на `setrate 1`
видно, как счётчик идёт по строкам. В игре на это смотрят редко, а вот в примерах этого
курса пошаговая кнопка делает ровно то же самое.

<Card kind="note">
**Ритм задают не скоростью.** Если программе нужно делать что-то раз в секунду,
это <Lesson lesson="wait/rhythm">`wait`</Lesson>, а не `setrate`: `wait` говорит о сроке
прямо, а замедление растягивает всю программу целиком и зависит от её длины.
</Card>

<Card kind="recap">
- `@ipt` — сколько инструкций процессор успевает за тик: 2, 8 или 25.
- `setrate число` меняет её на ходу, и `@ipt` обновляется сразу.
- Число прижимается к отрезку от 1 до предела блока: выше своей скорости обычный процессор
  не поднять.
- Поднимает скорость `setrate` только мировому процессору — до 1000.
- Смысл инструкции — разогнать мировой процессор; обычному замедление обычно ни к чему.
</Card>

<Card kind="task">
Поменяйте во втором примере `setrate 1` на `setrate 5` и запустите: итераций за секунду
станет заметно больше, а `скорость` покажет пятёрку.

Потом поставьте `setrate 0`. Скорость останется единицей, а не нулём: ноль прижимается
к нижней границе, и совсем остановить процессор этим нельзя — для этого есть
<Lesson lesson="stop">`stop`</Lesson>.
</Card>
