---
title: Флаг как метка
description: "Зачем метить юнитов, почему метку надо ставить всегда и как проверять, что юнит всё ещё ваш"
lead: "У каждого юнита есть число, которое игра никак не использует. Без него два процессора растащат один отряд"
difficulty: medium
sidebar:
    order: 4
---

import Example from '../../../../../example/Example.astro'
import LessonHead from '../../../../../course/LessonHead.astro'
import '../../../../../example/example.css'
import Card from '../../../../../course/Card.astro'
import Lesson from '../../../../../course/Lesson.astro'
import {FLAG} from '../../../../../course/scenes/ucontrol.js'

<LessonHead
    difficulty="medium"
    lead={frontmatter.lead}
    needs={[
        {lesson: 'ubind/object', title: 'Держать одного юнита'},
        {lesson: 'jump/guards', title: 'Вложенные условия и ранний выход'}
    ]}
/>

Перебор выдаёт юнитов по кругу, и программа каждый раз получает кого-то нового. Хуже того,
тот же перебор идёт и у соседнего процессора — и выдаёт ему **тех же самых** юнитов. Чтобы
поделить отряд, юнита **помечают**:

```
ucontrol flag 7 0 0 0 0
```

Теперь у него в `@flag` лежит семёрка. Прочитать её может любой процессор:

```
sensor метка @unit @flag
```

<Example scene={FLAG} world={true} />

Программа раздаёт номера: у кого метки нет (ноль) — тому выдаётся следующая, у кого есть —
тот пропускается. Три поли получают единицу, двойку и тройку, и `последний` останавливается
на трёх: помечать больше некого.

## Метку ставят всегда, а не когда понадобится

Легко решить, что разметка — это для сложных случаев: «у меня один процессор, я просто
буду брать своих перебором». Так не выйдет. `ubind @poly` у каждого процессора идёт
по **общему** списку всех поли команды, и счётчик у каждого свой. Два процессора
без меток неизбежно возьмут одного и того же юнита и будут <Lesson
lesson="ucontrol/control">отбирать его друг у друга</Lesson> — молча, без единой ошибки.

Поэтому разметка — это не приём на будущее, а обычный вид программы про юнитов:

```
ubind @poly
sensor метка @unit @flag
jump 0 notEqual метка 0
ucontrol flag 1 0 0 0 0
```

Пока юниты без метки, процессор их разбирает; размеченных пропускает. Чужие — те, у кого
метка не его, — он не тронет никогда.

<Card kind="error">
**Метку мало поставить, её надо проверять.** Программа, которая пометила юнита один раз
и дальше командует не глядя, рано или поздно скомандует чужому: юнит погиб, на его месте
в переборе оказался другой, а метку у него никто не спрашивал.

```
ubind @poly
sensor метка @unit @flag
jump 0 notEqual метка 1
ucontrol move 20 30 0 0 0
```

Три строки, и они обязательны: привязаться, спросить метку, уйти на начало, если метка
чужая. Всё остальное — уже после проверки.
</Card>

<Card kind="error">
**И метки мало.** Флаг говорит «этот юнит мой по уговору», но не говорит, что его не забрал
сосед: метку `ucontrol` не смотрит вовсе. Полная проверка — ещё и <Lesson
lesson="ucontrol/control">`@controller`</Lesson>:

```
sensor кто @unit @controller
jump 0 notEqual кто @this
```

Первая строка отсекает чужих по уговору, вторая — чужих по факту.
</Card>

## Зачем это нужно

Флаг — **единственная память, которая живёт на самом юните**, а не в процессоре и не
в ячейке. Отсюда и все его применения:

- **разделить отряд.** Метка `1` возит медь, метка `2` копает, метка `3` чинит. Каждый
  процессор смотрит на флаг и пропускает чужих;
- **не хватать одного и того же дважды.** Пометив юнита, программа видит, что он уже
  занят — даже если забыла его между итерациями;
- **передать юнита другому процессору.** Один помечает, другой читает: общаться через флаг
  дешевле, чем через ячейку памяти, и метка всегда при юните;
- **посчитать своих.** Перебор плюс проверка флага — и ясно, сколько юнитов уже разобрано
  по делам.

<Card kind="note">
Флаг — обычное число со всеми его свойствами, и в него влезает многое: например, номер
задачи и координата, упакованные вместе. Правда, «многое» упирается в то, сколько
<Lesson lesson="advanced/numbers">целых чисел помещается в число</Lesson> — 53 бита,
и на этом всё.
</Card>

## Чего флаг не умеет

<Card kind="error">
**Флаг не переживает гибель юнита.** Новый юнит того же типа приходит с нулём, и все метки
надо раздавать заново. Программа, которая раздаёт метки один раз на старте, после первой
же потери юнита останется с дырой в отряде.

Поэтому раздачу держат в цикле — как в примере: ноль означает «не размечен», и новичок
получает номер сам собой.
</Card>

Флаг не видит враг — как и всё остальное у чужих юнитов. Флаг не показывается в игре:
ни цифры над юнитом, ни подсветки. И флага нет у зданий: там роль метки играет
<Lesson lesson="write/cells">ячейка памяти</Lesson> или настройка блока.

И договариваться о числах приходится самим: игра не даст двум процессорам разные метки,
она просто хранит число. Два автора, поставившие единицу, получат ту же путаницу,
что и без меток вовсе.

<Card kind="recap">
- `ucontrol flag число` кладёт метку на юнита, `sensor @flag` её читает.
- Метку ставят **всегда**: иначе процессоры растащат один отряд.
- И всегда проверяют — метку перед работой, а `@controller` от перехвата.
- Ноль означает «не размечен» — с него начинают все юниты.
- Гибель стирает метку: раздачу держат в цикле.
</Card>

<Card kind="task">
Поменяйте в примере `jump 6 notEqual метка 0` на `jump 6 notEqual метка 99`: программа
решит, что размечать надо всех и каждую итерацию, и `последний` полетит вверх без остановки.

Потом верните как было и допишите после `ucontrol flag` строку `ucontrol move 3 3 0 0 0`:
размеченные юниты соберутся в одном углу — метка и команда обычно ходят вместе.
</Card>
