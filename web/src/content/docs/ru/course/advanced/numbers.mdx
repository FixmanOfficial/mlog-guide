---
title: Число внутри
description: "Из чего сделано число в mlog: двойная точность, 53 значащих бита, где целые перестают быть целыми"
lead: "Число в mlog одно на всё — дробное двойной точности. Отсюда и предел в 53 бита, и 0.1 + 0.2 ≠ 0.3"
difficulty: hard
sidebar:
    order: 2
---

import {Example} from '../../../../../example/Example.jsx'
import LessonHead from '../../../../../course/LessonHead.astro'
import '../../../../../example/example.css'
import Card from '../../../../../course/Card.astro'
import Lesson from '../../../../../course/Lesson.astro'
import {PRECISE} from '../../../../../course/scenes/advanced.js'

<LessonHead
    difficulty="hard"
    lead={frontmatter.lead}
    needs={[
        {lesson: 'basics/values', title: 'Число, объект и текст'},
        {lesson: 'op/integers', title: 'Целые, остаток и точность'}
    ]}
/>

Отдельного целого типа в mlog нет. Любое число — **дробное двойной точности**, то самое
`double`, что и в большинстве языков: 64 бита, из которых 52 отведены под мантиссу, 11 под
показатель степени и один под знак.

Из этого устройства следует всё, что дальше: и почему целые точны до определённого предела,
и почему `0.1 + 0.2` не равно `0.3`, и какие биты переживут упаковку.

<Example client:load scene={PRECISE} world={false} />

## 53 значащих бита

Мантисса хранит 52 бита, плюс один подразумеваемый — итого **53 значащих бита**. Пока число
помещается в них, оно целое и точное; дальше между соседними представимыми числами появляется
зазор.

| Число | Что с ним |
| --- | --- |
| до 2⁵³ − 1 | хранится точно, бит в бит |
| 2⁵³ = 9 007 199 254 740 992 | последнее целое, за которым начинается пропуск |
| 2⁵³ + 1 | **не существует**: округляется обратно к 2⁵³ |
| дальше | шаг растёт: сначала через одно, потом через два, четыре… |

В примере это видно прямо: `сОдной` — это 2⁵² с подмешанной единицей, и единица на месте.
А `безЕдиницы` — 2⁵³ с той же единицей, и её там нет: `проверка` равна нулю, то есть число
осталось ровно 2⁵³.

<Card kind="error">
**Ошибка не сообщается.** Ни ошибки, ни замечания: `op or` отработал, число получилось,
просто младший бит в него не поместился. Заметить это можно только сравнением — или когда
распакованное значение окажется не тем, что клали.
</Card>

## Какие биты безопасны

Для <Lesson lesson="op/bitwise">битовых операций</Lesson> правило простое:

- **биты 0–52 надёжны.** Их можно ставить, читать, сдвигать — число переживёт;
- **биты 53–63 существуют внутри операции, но не в переменной.** Игра считает битовые
  операции в 64-битном целом, а результат кладёт обратно в дробное — и старшие биты
  округляются.

```
op shl метка 1 52
op or упаковано упаковано метка
```

Так делать можно. А `op shl метка 1 53` уже нет: значение в переменную вернётся, но младшие
биты рядом с ним жить не смогут.

<Card kind="note">
Отсюда и бюджет упаковки: **53 бита на всё**. Например, четыре числа по 13 битов (каждое
до 8191) — помещаются; пять по 12 — тоже. А вот две координаты по 32 бита не поместятся
никак, хотя по отдельности каждая точна.
</Card>

## Дроби хранятся не точнее

Дробное число хранится теми же 53 битами, но уже не «сколько единиц», а «сколько
двоичных долей». Десятичная `0.1` в двоичной записи бесконечна — как `1/3` в десятичной, —
поэтому хранится ближайшее представимое значение.

В примере `дробь` равна `0.30000000000000004`, а `ошибка` — разница с настоящей `0.3` —
около 5.55 · 10⁻¹⁷.

<Card kind="error">
**Дробные числа нельзя сравнивать на равенство.** `op equal x 0.3` после сложения
`0.1 + 0.2` даст… единицу — и это не удача, а поблажка: `equal` в mlog сравнивает с допуском
10⁻⁶. Строгое `strictEqual` допуска не делает и ответит нулём.

Правило: дробное сравнивают либо обычным `equal`, либо разностью и порогом — но не строгим
равенством.
</Card>

## Бесконечность и NaN в переменную не ложатся

Деление на ноль даёт бесконечность, корень из отрицательного — NaN. Ни то, ни другое
переменная не хранит: при записи такое значение превращается в **пустоту**.

```
op div плохо 1 0
```

В `плохо` окажется `null`, а не «бесконечность». Поэтому проверка деления на ноль обычно
выглядит как проверка на пустоту после операции — или как проверка делителя до неё.

<Card kind="recap">
- Число в mlog одно: дробное двойной точности, 53 значащих бита.
- Целые точны до 2⁵³, дальше числа идут с пропусками.
- Для битовых операций надёжны биты 0–52; старшие живут только внутри операции.
- Дроби хранятся приближённо: `0.1 + 0.2` — это `0.30000000000000004`.
- Бесконечность и NaN в переменной становятся пустотой.
</Card>

<Card kind="task">
Поменяйте в примере `op shl безЕдиницы 1 53` на `op shl безЕдиницы 1 52`: `проверка` станет
отрицательной — число теперь меньше 2⁵³, зато единица в нём сохранилась.

Потом допишите `op div бесконечность 1 0` и посмотрите в таблицу переменных: там будет
`null`, а тип — пусто, а не число.
</Card>
