---
title: jump
description: Группа уроков про jump — единственную инструкцию, которая меняет порядок выполнения
lead: "Проверить условие и пойти не туда, куда пошёл бы обычно: ветвления и циклы"
sidebar:
    order: 0
    label: Обзор
---

import LessonList from '../../../../../course/LessonList.astro'

Пока в программе нет `jump`, она читается сверху вниз и по кругу — и делает одно и то же
при любых обстоятельствах. `jump` — единственная инструкция, которая это меняет.

Строка устроена так: **куда прыгнуть**, **какое условие** и **что с чем сравнить**.
Если условие верно, следующей выполнится строка, на которую показывает стрелка; если нет —
обычная следующая.

Из этого собирается всё остальное:

- **ветвление** — переход вперёд, через кусок программы;
- **цикл** — переход назад, к строке, которая уже выполнялась.

Никакой разницы для процессора между ними нет: и там, и там просто меняется номер следующей
строки. Поэтому и уроков всего три.

## Уроки

<LessonList locale="ru" group="jump" />

## Что нужно знать заранее

[«Как работает процессор»](/ru/course/basics/processor/) — что программа идёт по кругу
и что такое `@counter`. Сравнения те же, что у `op`, поэтому
[«Сравнения и логика»](/ru/course/op/logic/) будут кстати, но обязательными не считаются.
