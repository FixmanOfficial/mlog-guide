---
title: Write
description: Группа уроков про write — запись в ячейку и блок памяти, объекты в памяти и общая память нескольких процессоров
lead: "Переменные живут между итерациями, но не переживают правку программы. Память — переживает"
sidebar:
    order: 0
    label: Обзор
---

import LessonList from '../../../../../course/LessonList.astro'

`write` кладёт значение в память — ячейку или блок памяти — по адресу. Обратная сторона
[`read`](/ru/course/read/), и поля у неё те же, только первое поле теперь не «куда положить
прочитанное», а «что записать».

Память нужна там, где не хватает переменных:

- **пережить правку программы.** Переменные обнуляются, когда программу меняют; память —
  нет;
- **поделиться с соседом.** Двум процессорам не видны переменные друг друга, зато ячейка
  видна обоим;
- **хранить список.** Переменная держит одно значение, ячейка — 64, блок памяти — 512.

## Уроки

<LessonList locale="ru" group="write" />

## Что нужно знать заранее

[«Ячейка памяти»](/ru/course/read/cells/) — адреса, границы и чем пустота отличается
от нуля. Остальное уроки расскажут сами.
