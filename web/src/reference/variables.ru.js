/**
 * Переменные, которых нет в окне игры.
 *
 * Окно «Переменные» показывает список из `GlobalVars` — его снимает `gen-globals.mjs`.
 * Но три переменные живут не там, а в самом процессоре (`LExecutor.java:133-137`),
 * и в окне их не видно, хотя пишут о них чаще всего.
 */

export const PROCESSOR_RU = {
    '@counter': 'Номер инструкции, которая выполнится следующей. Единственная встроенная '
        + 'переменная, которую можно менять: запись в неё — это переход, а `set @counter 0` '
        + 'начинает программу заново.',
    '@unit': 'Юнит, взятый последним `ubind`. Именно ему адресованы `ucontrol` и `uradar`.',
    '@queries': 'Список, в который `query` складывает найденное. Есть только у процессора мира.'
}

/** Порядок тот же, что в исполнителе: счётчик, юнит, результаты запроса. */
export const PROCESSOR_VARS = [
    {name: '@counter', privileged: false},
    {name: '@unit', privileged: false},
    {name: '@queries', privileged: true}
]
